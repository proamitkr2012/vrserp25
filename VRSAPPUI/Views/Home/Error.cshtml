@{
    ViewData["Title"] = "Error";
}


<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f0f0f0;
        margin: 0;
        padding: 0;
    }

    .alert {
        background-color: #f44336; /* Red background */
        color: white; /* White text */
        text-align: center;
        padding: 15px;
        position: fixed; /* Fixed position at the top */
        width: 100%; /* Full width */
        top: 0; /* At the top */
        z-index: 1000; /* Higher z-index to ensure it's on top of other content */
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* Shadow */
        transition: top 0.3s ease; /* Smooth transition */
    }

    .container {
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 80%;
        margin: 80px auto 20px; /* Adjusted margin for better spacing */
    }

    h1 {
        color: #ff6347;
        font-size: 2.5em; /* Larger heading */
        margin-bottom: 15px; /* Space below heading */
    }

    p {
        color: #555;
        line-height: 1.8;
        font-size: 1.1em; /* Slightly larger font size */
        margin-bottom: 10px; /* Space below paragraphs */
    }
    /* Button styles */
    .btn {
        display: inline-block;
        padding: 10px 20px;
        background-color: #4CAF50;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

        .btn:hover {
            background-color: #45a049;
        }
</style>

<div class="alert">
    <strong>Warning:</strong> We are currently experiencing technical difficulties. Our team is working on resolving the issue as soon as possible. We apologize for any inconvenience caused.
</div>

<div class="container">
    <h1>Technical Glitch Warning</h1>
    <p>We are currently not having the proper data. It will get resolved as soon as the data will be fetched.</p>
    <p>We apologize for any inconvenience caused. Please check back shortly or you can contact on support portal.</p>
    <a href="#" class="btn">Refresh Page</a>
</div>

